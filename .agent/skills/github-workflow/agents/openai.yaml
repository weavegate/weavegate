interface:
  display_name: "GitHub Workflow"
  short_description: "Ship weavegate changes through GitHub"
  default_prompt: "Use $github-workflow to create the issue, branch, commit, push, and pull request for this weavegate change."
