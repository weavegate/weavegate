interface:
  display_name: "GitHub Workflow"
  short_description: "Prepare, publish, update, or merge Weavegate work"
  default_prompt: "Use $github-workflow to handle the current prepare, publish, update, or merge stage for this weavegate change."
